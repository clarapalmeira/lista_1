#include <stdio.h>

int main() {
    int numero;

   
    printf("Digite um numero: ");
    scanf("%d", &numero);

    
    printf("O modulo (valor absoluto) do numero digitado: %d\n", abs(numero));

    return 0;
}
